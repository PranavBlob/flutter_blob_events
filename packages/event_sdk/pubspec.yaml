name: event_sdk
description: Flexible multi-platform event SDK core (router + shared API).
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.10.0

resolution: workspace

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
