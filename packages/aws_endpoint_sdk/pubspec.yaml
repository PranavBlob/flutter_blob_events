name: aws_endpoint_sdk
description: HTTP endpoint adapter for event_sdk (MobileTrackingEvent format).
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.10.0
  flutter: ">=3.38.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  event_sdk:
    path: ../event_sdk

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
