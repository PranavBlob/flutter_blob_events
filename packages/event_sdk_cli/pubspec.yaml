name: event_sdk_cli
description: CLI to init/add/remove Event SDK platforms in a Flutter app.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.10.0

resolution: workspace

executables:
  event_sdk_cli:

dependencies:
  args: ^2.7.0
  path: ^1.9.0
  yaml: ^3.1.3
  yaml_edit: ^2.2.2

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
